# RETO_SEM_15 Uso de API's con la página Open Wheather.
Se genero la llave par usar la API de la pagina, con la versión 2.5, ya que no fue posible usar la versión 3.0.
Se inición con import request, los datos de latitud y longitud de una ubicación geográfica, y la URL con éstos datos y la APY_key.
Se hizo la petición con request y se verifica la respuesta, para establecer si es exitosa, con if y else.
Para los datos de clima con el pronóstico de cada 3 horas con if, for y else.
De nuevo el programa se desarrollo con el apoyo de la IA.
